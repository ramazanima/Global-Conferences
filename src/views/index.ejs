<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Web App</title>
</head>
<body>
    <h1>
        Welcome to <%=title%>
    </h1>
    <ul>
            <% data.map((i) =>{
               %><li><%=i%></li><%
            })
        %>
    </ul>

    <ul>
        <% names.map((i) =>{ %>
            <li><%=i%></li>
      <%})%>
    </ul>
</body>
</html>

